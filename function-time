// PLAYING WITH FUNCTIONS

const calculateArea = function(width, height) {
  const area = width * height; 
  console.log(area);
} 

calculateArea(80,33); 

// longform function, returns 2640 or 80 * 33

function nedGreeting() { 
console.log('Howdy diddly!')
}

nedGreeting()

// longform function, returns 'Howdy diddly!'

function sayThanks(name) {
  console.log('Thank you for your purchase '+name+'! We appreciate your business.');
}
sayThanks('Cole'); 

// can pass arguments through 

function bigFan(number){
  console.log(`Wow, you've read this book ${number} times, you must be a big fan!`)
}
bigFan(30)

// works with numbers too 

// renaming long functions

const checkThatTwoPlusTwoEqualsFourAMillionTimes = () => {
  for(let i = 1; i <= 1000000; i++) {
    if ( (2 + 2) != 4) {
      console.log('Something has gone very wrong :( ');
    }
  }
}
const is2p2 = checkThatTwoPlusTwoEqualsFourAMillionTimes // renames the function with the long name inside a new variable 
is2p2();    // calls the old function 
console.log(is2p2.name);
